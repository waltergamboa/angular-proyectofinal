import { Letras20Directive } from './letras-20.directive';

describe('Letras20Directive', () => {
  it('should create an instance', () => {
    //const directive = new Letras20Directive();
    //expect(directive).toBeTruthy();
  });
});
